[project]
name = "hybrid"
version = "0.1.0"
description = "A hybrid solution of artificial coding and vibe coding"
requires-python = ">=3.14"
dependencies = [
    "manim>=0.20.1",
    "pywebview[gtk]>=6.2.1",
    "plotly>=5.0.0",
]
